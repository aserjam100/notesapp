# Always save exact versions in package-lock.json
save-exact=true

# Disable audit on install (faster Vercel builds)
audit=false

# Ignore peer dependency conflicts (useful for eslint chains)
legacy-peer-deps=true